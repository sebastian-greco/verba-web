# Polar.sh — get these from https://polar.sh → Settings → Access Tokens / Products
POLAR_ACCESS_TOKEN=polar_pat_REPLACE_ME
POLAR_PRODUCT_ID=prod_REPLACE_ME
POLAR_ORG_ID=REPLACE_ME
POLAR_BENEFIT_ID=REPLACE_ME

# Optional: Set to 'sandbox' to test with Polar's sandbox environment. Defaults to 'production'.
# POLAR_ENV=sandbox
